<div class="container p-5" >
<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
        <%= attachment_image_tag @user, :profile_image, :fill, 20, 20, fallback: "no_image.jpg" %>
      <table class="table">
        <tbody>
          <tr>
            <th>name</th>
            <th></th>
          </tr>
          <tr>
            <th>introdacution</th>
          </tr>
        </tbody>
      </table>

      <div class="row">
        <div class="btn btn-outline-secondary btn-block">
          <%= link_to "aikon", @user_show_path %>
        </div>

        <h2>New book</h2>

        <%= form_with model: @book, local: true do |f| %>
          <h4>title</h4>
          <%= f.text_field :title %>
          <h4>Opinion</h4>
          <%= f.text_area :body %>
          <%= f.submit 'Create Book' %>
        <%end%>


      </div>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>

      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>

            <th colspan="3"></th>

          </tr>
        </thead>

        <tbody>
        <% @books.each do |book| %>
          <tr>
            <tb></tb>
            <td><%= attachment_image_tag @user, :profile_image, :fill, 5, 5, fallback: "no_image.jpg" %></td>
            <td>aaaa<% link_to "book.title, book_path(book.id) %>  </td>
            <td>aaaa</td>
          </tr>
        <%end%>
        </tbody>

      </table>

    </div>
  </div>
</div>