<div class="container p-5" >
<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      
      <table class="table">
        <tbody>
          <tr>
            <th>name</th>
            <th></th>
          </tr>
          <tr>
            <th>introdacution</th>
          </tr>
        </tbody>
      </table>

      <div class="row">
        <div class="btn btn-outline-secondary btn-block">
          <%= link_to "aikon", @user_show_path %>
        </div>

        <h2>New book</h2>

        <%= form_with model: @book, local: true do |f| %>
          <h4>title</h4>
          <%= f.text_field :title %>
          <h4>Opinion</h4>
          <%= f.text_area :body %>
          <%= f.submit 'Create Book' %>
        <%end%>


      </div>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <%= attachment_image_tag user,　:profile_image, :fill, 20, 20, fallback: "no_image.jpg"  %>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>

            <th>Image</th>
            <th>name</th>
            <th></th>
            <th colspan="3"></th>

          </tr>
        </thead>

        <tbody>
          <tr>
            <tb></tb>
            <td>投稿ユ</td>
            <td>ddd<%= @users.name %> </td>
            <td><%= link_to "show",users_path(params[:id]) %></td>
          </tr>
        </tbody>

      </table>

    </div>
  </div>
</div>