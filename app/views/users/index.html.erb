<div class="container p-5" >
<div class="container">
  <div class="row">
    <div class="col-md-3">
     <%= render 'users/list',users:@users,user:@user,book:@book %>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>

      <table class="table table-hover table-inverse">
        <thead>
          <tr>

            <th>Image</th>
            <th>name</th>
            <th></th>
            <th colspan="3"></th>

          </tr>
        </thead>

        <tbody>
        <% @books.each do |book| %>
          <tr>
            <tb></tb>

            <td><%= attachment_image_tag book.user, :profile_image, :fill, 20, 20, fallback: "no_image.jpg"  %></td>
            <td>
              <%= book.user.name %>
            </td>
            <td><%= link_to "show", user_path(book.user.id) %></td>

          </tr>
        <%end%>
        </tbody>


      </table>

    </div>
  </div>
</div>