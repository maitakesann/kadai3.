<div class="container p-5" >
<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <% @users.each do |user| %>
      <%= attachment_image_tag user,:profile_image, :fill, 20, 20, fallback: "no_image.jpg"  %>

      <table class="table">
        <tbody>
          <tr>
            <th>name</th>
            <th><%= user.name %></th>

            <th></th>
          </tr>
          <tr>
            <th>introdacution</th>
          </tr>
        </tbody>

      </table>
      <%end%>

      <div class="row">
        <div class="btn btn-outline-secondary btn-block">
          <%= link_to "aikon", edit_user_path(@user.id)  %>
        </div>

        <h2>New book</h2>

        <%= form_with model: @book, local: true do |f| %>
          <h4>title</h4>
          <%= f.text_field :title %>
          <h4>Opinion</h4>
          <%= f.text_area :body %>
          <%= f.submit 'Create Book' %>
        <%end%>


      </div>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>

      <table class="table table-hover table-inverse">
        <thead>
          <tr>

            <th>Image</th>
            <th>name</th>
            <th></th>
            <th colspan="3"></th>

          </tr>
        </thead>

        <tbody>
        <% @books.each do |book| %>
          <tr>
            <tb></tb>

            <td><%= attachment_image_tag book.user, :profile_image, :fill, 20, 20, fallback: "no_image.jpg"  %></td>
            <td>
              <%= book.user.name %>
            </td>
            <td><%= link_to "show", user_path(book.user.id) %></td>

          </tr>
        <%end%>
        </tbody>


      </table>

    </div>
  </div>
</div>